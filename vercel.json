{
  "version": 2,
  "builds": [
    {
      "src": "./index.html",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "chmod +x node_modules/.bin/vite && npm run build",
        "outputDirectory": "dist"
      }
    }
  ],
  "framework": "vite"
}